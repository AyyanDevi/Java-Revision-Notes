public class matrix {
	public static void main(String[] args) {
		String str = "Hello, World!";
		// Loop through the string to access each character
		for (int i = 0; i < str.length(); i++) {
			char a = str.charAt(i);
			System.out.println("Character at index " + i + ": " + a);
		}
	}
}
